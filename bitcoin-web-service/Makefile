# Auto-generated by Baton

BERLIOZ_GROUP     := udacity
BERLIOZ_NAME      := berlioz-example
DOCKER_REPO       := $(BERLIOZ_GROUP)/$(BERLIOZ_NAME)
SERVICE_NAME      := $(BERLIOZ_NAME)
VERSION           ?= $(shell git rev-parse --short HEAD)
CONDUCTOR_APP_ID  := 

build:
	docker build -t udacity/berlioz-example:$(VERSION) .

run:
	docker run -p 5000:5000 udacity/berlioz-example

push:
	docker push udacity/berlioz-example:$(VERSION)

deploy: push
	baton conductor deploy --app berlioz-example --env production --image udacity/berlioz-example


